// istilah primitif digunakan kerena hanya menyimpan 1 nilai tunggal, data bukan object dan tidak memiliki method

// string
var name = "ahmad arifin";

// numbers
var age = 20;
var price = 1.51;

// boolean
var isExist = true;

// null
var object = null;

// undefined
var flag = undefined;
var ref; //setara dengan kode diatas yaitu menghasilkan undefinned