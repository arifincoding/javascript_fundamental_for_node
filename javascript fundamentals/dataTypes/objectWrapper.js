// object wrapper berfungsi untuk membuat sebuah type data primitive bisa diberi akses untuk mengakses kemampuan super yaitu method


let str = "hello";

// method toUpperCase
console.log(str.toUpperCase()); //HELLO

// method toLowerCase
console.log(str.toLowerCase()); //hello