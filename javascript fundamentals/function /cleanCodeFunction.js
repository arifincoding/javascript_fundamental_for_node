// selalu dekrarasi local variabel minimalisir global variabel

// gunakan named function expression

const short = function longuniqueMoreDescriptiveLexicalFoo() {

}

// use default parameter

function myFunction(x, y) {
    if (y === undefined) {
        y = 0;
    }
}

// fungsi bukan statement
let text;
if (currentUser) {
    test = () => {
        console.log('Yup.');
    };
}